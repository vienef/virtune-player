import { Lato, Pattaya } from 'next/font/google';

export const lato = Lato({
  weight: ['400', '900'],
  subsets: ['latin'],
});

export const pattaya = Pattaya({
  weight: ['400'],
  subsets: ['latin'],
});
